export type SwatchType = {
    id: string,
    hex: string,
    rgb: string,
    rgbArray: Array<number>,
    fontColor: string
};

export type PaletteType = {
    name: string,ndkm
    colors: Array<string>
};